<!DOCTYPE html>
<html lang="bn">
<head>
  <meta charset="UTF-8">
  <title>আমার বট</title>
  <style>
    body { font-family: Arial, sans-serif; text-align: center; padding: 30px; }
    input, button { padding: 10px; margin: 5px; font-size: 16px; }
    #result { margin-top: 20px; font-size: 20px; font-weight: bold; color: green; }
  </style>
</head>
<body>
  <h1>📢 আমার প্রেডিকশন বট</h1>
  <p>পিরিয়ড নাম্বার লিখে সাবমিট করুন:</p>
  <input type="number" id="period" placeholder="Period Number দিন">
  <button onclick="predict()">দেখুন কি আসবে</button>

  <div id="result"></div>

  <script>
    function predict() {
      let period = document.getElementById("period").value;
      if (!period) {
        document.getElementById("result").innerHTML = "⚠️ আগে নাম্বার দিন!";
        return;
      }
      // ডেমো প্রেডিকশন (Random)
      let options = ["Red", "Green", "Big", "Small"];
      let next = options[Math.floor(Math.random() * options.length)];
      document.getElementById("result").innerHTML = 
        "পরবর্তী আসতে পারে ➝ " + next;
    }
  </script>
</body>
</html>
